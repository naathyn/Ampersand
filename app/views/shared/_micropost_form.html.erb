<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, rows: "3", placeholder: "Speak your mind..." %>
  </div>
  <%= f.submit "Share", class: "btn btn-large btn-primary" %>
<% end %>
