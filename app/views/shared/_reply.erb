<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, rows: "3", placeholder: "Send them a reply..." %>
  </div>
  <%= f.submit "Reply", class: "btn btn-primary" %>
<% end %>
