<% if @atreply_items.any? && current_user?(@user) %>
	<div class="well-header" style="margin-top:10px;"><%= pluralize(@user.atreply.count, "@reply") %> for you</div>
		<ol class="microposts">
    	<%= render partial: 'shared/atreply_item', collection: @atreply_items %>
  	</ol>
  	<%= will_paginate @atreply_items %>
	<div class="well-bottom" style="margin-bottom:15px;"></div>
<% end %>
