<%= form_for(current_user.microposts.build) do |f| %>

    <%= f.text_area :content,
        placeholder: "Speak up, the world needs listeners..." %>

  <%= f.submit "Share", class: "btn btn-primary", 
                        style: "padding-left:10px;" %>
<% end %>
