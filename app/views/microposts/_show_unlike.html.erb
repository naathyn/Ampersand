<%= form_for(current_user.fans.find_by(like_id: @micropost),
            html: { method: :delete },
            remote: true) do |f| %>
  <%= f.submit "Liked", class: "btn btn-large btn-default" %>
<% end %>
