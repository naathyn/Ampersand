<legend> Tags </legend>
<ol id="tags">
  <% @user.tags.each do |tag| %>
    <li id="<%= tag.id %>"><i class="icon-tags"></i>
      <%= link_to "#{tag.name} (#{tag.tagging_count})", tag_path(tag) %>
    </li>
  <% end %>
</ol>
