<%= form_for(current_user.microposts.build) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

    <%= f.text_area :content,
      placeholder: "Reply to @#{item.user.name}", value: "@#{item.user.name} " %>

  <%= f.submit "Reply", class: "btn btn-primary" %>
<% end %>
