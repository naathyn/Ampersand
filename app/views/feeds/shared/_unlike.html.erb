<%= form_for(current_user.fans.find_by(like_id: item.id),
        html: { method: :delete },
        remote: true) do |f| %>
    <i class="icon-heart"></i>
  <%= f.submit "Liked", class: "btn-link shown" %>
<% end %>
