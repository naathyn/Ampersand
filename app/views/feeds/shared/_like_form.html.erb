<% if item.liked_by?(current_user) %>
  <%= render partial: 'feeds/shared/unlike', object: item, as: :item %>
<% else %>
  <%= render partial: 'feeds/shared/like', object: item, as: :item %>
<% end %>
