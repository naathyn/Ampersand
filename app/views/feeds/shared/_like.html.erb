<li id="like_form">
  <%= form_for(current_user.opinions.build(like_id: item.id),
               remote: true) do |f| %>
        <div><%= f.hidden_field :like_id %></div>
      <i class="icon-thumbs-up"></i>
    <%= f.submit "Like", class: "btn-link shown" %>
  <% end %>
</li>
