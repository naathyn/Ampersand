<% if @replies.any? %>

  <% @replies.each do |reply| %>
    <%= render partial: 'feeds/share_item', object: reply, as: "item" %>

  <% end %>

    <div class="well-bottom">
      <a href="#" alt="Top"><i class="icon-arrow-up"></i></a>
    </div>

  <%= will_paginate @replies %>

<% end %>
