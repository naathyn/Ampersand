<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <title> <%= full_title(yield(:title)) %> </title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "A Social Platform in development" %>">
    <meta name="author" content="<%= content_for?(:author) ? yield(:author) : "Nathan Couch" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
    <%= yield(:head) %>
  </head>
  <body class="body-<%= controller_name %>" id="<%= action_name %>">
    <%= render 'shared/modal' %>

    <header class="navbar navbar-inverse navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container-fluid">
          <%= render 'layouts/navigation' %>
        </div>
      </nav>
    </header>

    <section class="page-wrapper" id="main" role="main">
      <%= render 'layouts/nojs_warning' %>
      <%= render 'layouts/messages' %>
      <%= yield %>
      <%= debug(params) if Rails.env.development? %>
    </section>

    <footer class="footer">
      <div class="container">
        <%= render 'layouts/footer' %>
      </div>
    </footer>

  </body>
</html>
