<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <% if Rails.env.production? %>
      <meta name="google-site-verification"
            content="I0NixjSmlfnfKTF5fINNtQxaktiCwun9d46jsU11l3g" />
    <% end %>
    <title> <%= full_title(yield(:title)) %> </title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "A Social Platform in development" %>">
    <meta name="author" content="<%= content_for?(:author) ? yield(:author) : "Nathan Couch" %>">
      <%= stylesheet_link_tag "application", media: "all" %>
      <%= javascript_include_tag "application" %>
      <%= csrf_meta_tags %>
      <%= yield(:head) %>
    </head>
    <body class="<%= viewing_blog? %>">
      <%= render 'shared/modal' %>
      <header class="navbar navbar-inverse navbar-fixed-top">
        <nav class="navbar-inner">
          <div class="container-fluid">
            <%= render 'layouts/navigation' %>
          </div>
        </nav>
      </header>
      <div class="page-wrapper" id="main" role="main">
        <%= render 'layouts/nojs_warning' %>
        <%= render 'layouts/messages' %>
        <%= yield %>
        <%= debug(params) if Rails.env.development? %>
      </div>
      <footer class="footer">
        <div class="container">
          <%= render 'layouts/footer' %>
      </div>
    </footer>
  </body>
</html>
