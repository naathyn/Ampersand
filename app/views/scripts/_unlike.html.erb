<%= form_for(current_user.fans.find_by_like_id(@micropost),
             html: { method: :delete },
             remote: true) do |f| %>
  <li><i class="icon-heart"></i>
  <%= f.submit "Unlike", class: "btn-link" %></li>
<% end %>
