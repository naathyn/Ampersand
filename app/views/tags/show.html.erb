<% content_for :head do %>
  <% provide(:title, @title) %>
<% end %>

<p><%= link_to 'Back Blogs', :blogs_users %></p>

<ul class="nav nav-tabs">
  <li class="active"><a href="#tag"><%= @tag.name %></a></li>
</ul>
   
<div class="tab-content">

  <div class="tab-pane active" id="tag">
    <%= render @blogs %>
  </div>
</div>
