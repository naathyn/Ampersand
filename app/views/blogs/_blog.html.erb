<div id="<%= blog.id %>" class="well-body well-inner blog-wrapper">

  <h2 class="title">
      <%= link_to blog.title, blog %>

    <% if current_user?(blog.user) %>
      <%= render( partial: ('blogs/actions'), locals: { blog: blog } ) %>
    <% end %>

    <span class="blog-timestamp">
      <%= blog.timestamp %>
    </span>
  </h2>

  <div class="format">
    <%= auto_link(blog.content, html: { target: '_blank' }) do |content|
        truncate(content, length: 30) end %>
  </div>

  <p class="location">By <%= link_to blog.user.realname, blog_user_path(blog.user) %></p>

  <span class="bio">
    <% if blog.has_photo? %>
      <%= render(partial:("blogs/photo"), locals: { item: blog }) %> <br />
    <% end %>

    <% unless blog.tags.empty? %>
      <% blog.tags.each do |tag| %>
        <i class="icon-tag"></i> <%= link_to tag.name, tag %>
      <% end %>
    <% else %>
      untagged
    <% end %>
  </span>

</div>
